import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const Ty = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = Ty.actions

export default Ty.reducer